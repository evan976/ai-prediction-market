/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  type Address,
  containsBytes,
  fixEncoderSize,
  getBytesEncoder,
  type ReadonlyUint8Array,
} from '@solana/kit'
import type {
  ParsedBetNoInstruction,
  ParsedBetYesInstruction,
  ParsedClaimInstruction,
  ParsedCreateMarketInstruction,
  ParsedResolveInstruction,
} from '../instructions'

export const AI_PREDICTION_MARKET_PROGRAM_ADDRESS =
  '62sSE2g4sBQ7Av2mmhasTDP3zWA7QBpf9wa7nDN1rwj4' as Address<'62sSE2g4sBQ7Av2mmhasTDP3zWA7QBpf9wa7nDN1rwj4'>

export enum AiPredictionMarketAccount {
  BetRecord,
  Market,
}

export function identifyAiPredictionMarketAccount(
  account: { data: ReadonlyUint8Array } | ReadonlyUint8Array,
): AiPredictionMarketAccount {
  const data = 'data' in account ? account.data : account
  if (
    containsBytes(
      data,
      fixEncoderSize(getBytesEncoder(), 8).encode(
        new Uint8Array([144, 217, 102, 109, 200, 164, 66, 178]),
      ),
      0,
    )
  ) {
    return AiPredictionMarketAccount.BetRecord
  }
  if (
    containsBytes(
      data,
      fixEncoderSize(getBytesEncoder(), 8).encode(
        new Uint8Array([219, 190, 213, 55, 0, 227, 198, 154]),
      ),
      0,
    )
  ) {
    return AiPredictionMarketAccount.Market
  }
  throw new Error(
    'The provided account could not be identified as a aiPredictionMarket account.',
  )
}

export enum AiPredictionMarketInstruction {
  BetNo,
  BetYes,
  Claim,
  CreateMarket,
  Resolve,
}

export function identifyAiPredictionMarketInstruction(
  instruction: { data: ReadonlyUint8Array } | ReadonlyUint8Array,
): AiPredictionMarketInstruction {
  const data = 'data' in instruction ? instruction.data : instruction
  if (
    containsBytes(
      data,
      fixEncoderSize(getBytesEncoder(), 8).encode(
        new Uint8Array([102, 171, 136, 171, 215, 44, 89, 219]),
      ),
      0,
    )
  ) {
    return AiPredictionMarketInstruction.BetNo
  }
  if (
    containsBytes(
      data,
      fixEncoderSize(getBytesEncoder(), 8).encode(
        new Uint8Array([32, 212, 81, 46, 206, 147, 234, 28]),
      ),
      0,
    )
  ) {
    return AiPredictionMarketInstruction.BetYes
  }
  if (
    containsBytes(
      data,
      fixEncoderSize(getBytesEncoder(), 8).encode(
        new Uint8Array([62, 198, 214, 193, 213, 159, 108, 210]),
      ),
      0,
    )
  ) {
    return AiPredictionMarketInstruction.Claim
  }
  if (
    containsBytes(
      data,
      fixEncoderSize(getBytesEncoder(), 8).encode(
        new Uint8Array([103, 226, 97, 235, 200, 188, 251, 254]),
      ),
      0,
    )
  ) {
    return AiPredictionMarketInstruction.CreateMarket
  }
  if (
    containsBytes(
      data,
      fixEncoderSize(getBytesEncoder(), 8).encode(
        new Uint8Array([246, 150, 236, 206, 108, 63, 58, 10]),
      ),
      0,
    )
  ) {
    return AiPredictionMarketInstruction.Resolve
  }
  throw new Error(
    'The provided instruction could not be identified as a aiPredictionMarket instruction.',
  )
}

export type ParsedAiPredictionMarketInstruction<
  TProgram extends string = '62sSE2g4sBQ7Av2mmhasTDP3zWA7QBpf9wa7nDN1rwj4',
> =
  | ({
      instructionType: AiPredictionMarketInstruction.BetNo
    } & ParsedBetNoInstruction<TProgram>)
  | ({
      instructionType: AiPredictionMarketInstruction.BetYes
    } & ParsedBetYesInstruction<TProgram>)
  | ({
      instructionType: AiPredictionMarketInstruction.Claim
    } & ParsedClaimInstruction<TProgram>)
  | ({
      instructionType: AiPredictionMarketInstruction.CreateMarket
    } & ParsedCreateMarketInstruction<TProgram>)
  | ({
      instructionType: AiPredictionMarketInstruction.Resolve
    } & ParsedResolveInstruction<TProgram>)
