/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  combineCodec,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  fixDecoderSize,
  fixEncoderSize,
  getAddressEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getProgramDerivedAddress,
  getStructDecoder,
  getStructEncoder,
  getU64Decoder,
  getU64Encoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  transformEncoder,
  type WritableAccount,
  type WritableSignerAccount,
} from '@solana/kit'
import { AI_PREDICTION_MARKET_PROGRAM_ADDRESS } from '../programs'
import {
  expectAddress,
  getAccountMetaFactory,
  type ResolvedAccount,
} from '../shared'

export const BET_YES_DISCRIMINATOR = new Uint8Array([
  32, 212, 81, 46, 206, 147, 234, 28,
])

export function getBetYesDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(BET_YES_DISCRIMINATOR)
}

export type BetYesInstruction<
  TProgram extends string = typeof AI_PREDICTION_MARKET_PROGRAM_ADDRESS,
  TAccountMarket extends string | AccountMeta<string> = string,
  TAccountBettor extends string | AccountMeta<string> = string,
  TAccountBetRecord extends string | AccountMeta<string> = string,
  TAccountSystemProgram extends
    | string
    | AccountMeta<string> = '11111111111111111111111111111111',
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountMarket extends string
        ? WritableAccount<TAccountMarket>
        : TAccountMarket,
      TAccountBettor extends string
        ? WritableSignerAccount<TAccountBettor> &
            AccountSignerMeta<TAccountBettor>
        : TAccountBettor,
      TAccountBetRecord extends string
        ? WritableAccount<TAccountBetRecord>
        : TAccountBetRecord,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >

export type BetYesInstructionData = {
  discriminator: ReadonlyUint8Array
  amount: bigint
}

export type BetYesInstructionDataArgs = { amount: number | bigint }

export function getBetYesInstructionDataEncoder(): FixedSizeEncoder<BetYesInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ['discriminator', fixEncoderSize(getBytesEncoder(), 8)],
      ['amount', getU64Encoder()],
    ]),
    (value) => ({ ...value, discriminator: BET_YES_DISCRIMINATOR }),
  )
}

export function getBetYesInstructionDataDecoder(): FixedSizeDecoder<BetYesInstructionData> {
  return getStructDecoder([
    ['discriminator', fixDecoderSize(getBytesDecoder(), 8)],
    ['amount', getU64Decoder()],
  ])
}

export function getBetYesInstructionDataCodec(): FixedSizeCodec<
  BetYesInstructionDataArgs,
  BetYesInstructionData
> {
  return combineCodec(
    getBetYesInstructionDataEncoder(),
    getBetYesInstructionDataDecoder(),
  )
}

export type BetYesAsyncInput<
  TAccountMarket extends string = string,
  TAccountBettor extends string = string,
  TAccountBetRecord extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  market: Address<TAccountMarket>
  bettor: TransactionSigner<TAccountBettor>
  betRecord?: Address<TAccountBetRecord>
  systemProgram?: Address<TAccountSystemProgram>
  amount: BetYesInstructionDataArgs['amount']
}

export async function getBetYesInstructionAsync<
  TAccountMarket extends string,
  TAccountBettor extends string,
  TAccountBetRecord extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof AI_PREDICTION_MARKET_PROGRAM_ADDRESS,
>(
  input: BetYesAsyncInput<
    TAccountMarket,
    TAccountBettor,
    TAccountBetRecord,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  BetYesInstruction<
    TProgramAddress,
    TAccountMarket,
    TAccountBettor,
    TAccountBetRecord,
    TAccountSystemProgram
  >
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? AI_PREDICTION_MARKET_PROGRAM_ADDRESS

  // Original accounts.
  const originalAccounts = {
    market: { value: input.market ?? null, isWritable: true },
    bettor: { value: input.bettor ?? null, isWritable: true },
    betRecord: { value: input.betRecord ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  }
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >

  // Original args.
  const args = { ...input }

  // Resolve default values.
  if (!accounts.betRecord.value) {
    accounts.betRecord.value = await getProgramDerivedAddress({
      programAddress,
      seeds: [
        getBytesEncoder().encode(
          new Uint8Array([98, 101, 116, 95, 114, 101, 99, 111, 114, 100]),
        ),
        getAddressEncoder().encode(expectAddress(accounts.market.value)),
        getAddressEncoder().encode(expectAddress(accounts.bettor.value)),
      ],
    })
  }
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      '11111111111111111111111111111111' as Address<'11111111111111111111111111111111'>
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId')
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.market),
      getAccountMeta(accounts.bettor),
      getAccountMeta(accounts.betRecord),
      getAccountMeta(accounts.systemProgram),
    ],
    data: getBetYesInstructionDataEncoder().encode(
      args as BetYesInstructionDataArgs,
    ),
    programAddress,
  } as BetYesInstruction<
    TProgramAddress,
    TAccountMarket,
    TAccountBettor,
    TAccountBetRecord,
    TAccountSystemProgram
  >)
}

export type BetYesInput<
  TAccountMarket extends string = string,
  TAccountBettor extends string = string,
  TAccountBetRecord extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  market: Address<TAccountMarket>
  bettor: TransactionSigner<TAccountBettor>
  betRecord: Address<TAccountBetRecord>
  systemProgram?: Address<TAccountSystemProgram>
  amount: BetYesInstructionDataArgs['amount']
}

export function getBetYesInstruction<
  TAccountMarket extends string,
  TAccountBettor extends string,
  TAccountBetRecord extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof AI_PREDICTION_MARKET_PROGRAM_ADDRESS,
>(
  input: BetYesInput<
    TAccountMarket,
    TAccountBettor,
    TAccountBetRecord,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): BetYesInstruction<
  TProgramAddress,
  TAccountMarket,
  TAccountBettor,
  TAccountBetRecord,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? AI_PREDICTION_MARKET_PROGRAM_ADDRESS

  // Original accounts.
  const originalAccounts = {
    market: { value: input.market ?? null, isWritable: true },
    bettor: { value: input.bettor ?? null, isWritable: true },
    betRecord: { value: input.betRecord ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  }
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >

  // Original args.
  const args = { ...input }

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      '11111111111111111111111111111111' as Address<'11111111111111111111111111111111'>
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId')
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.market),
      getAccountMeta(accounts.bettor),
      getAccountMeta(accounts.betRecord),
      getAccountMeta(accounts.systemProgram),
    ],
    data: getBetYesInstructionDataEncoder().encode(
      args as BetYesInstructionDataArgs,
    ),
    programAddress,
  } as BetYesInstruction<
    TProgramAddress,
    TAccountMarket,
    TAccountBettor,
    TAccountBetRecord,
    TAccountSystemProgram
  >)
}

export type ParsedBetYesInstruction<
  TProgram extends string = typeof AI_PREDICTION_MARKET_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>
  accounts: {
    market: TAccountMetas[0]
    bettor: TAccountMetas[1]
    betRecord: TAccountMetas[2]
    systemProgram: TAccountMetas[3]
  }
  data: BetYesInstructionData
}

export function parseBetYesInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedBetYesInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 4) {
    // TODO: Coded error.
    throw new Error('Not enough accounts')
  }
  let accountIndex = 0
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!
    accountIndex += 1
    return accountMeta
  }
  return {
    programAddress: instruction.programAddress,
    accounts: {
      market: getNextAccount(),
      bettor: getNextAccount(),
      betRecord: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getBetYesInstructionDataDecoder().decode(instruction.data),
  }
}
